#include<stdio.h>
int main(int argc, char** argv)
{
	int x, y;// x, y are integer type
	x = 5; // x is positive
	y = -5; // y is negative
	
	printf (" The value of x is %d",x);
	printf(" \n The value of y is %d",y);
	
	printf ("\n The value of x is %d and value of y is %d", x, y);
}

