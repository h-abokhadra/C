#include <stdio.h>
int main(int argc, char** argv)
{
	int i, n;   // i is the loop variable
	printf("Enter the value of n :\n");
	scanf("%d", &n);
		for(i=1;i<=n;i++)
			printf(" %d \n", i);
return 0;
}
