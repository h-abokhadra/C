#include <stdio.h>
int main(int argc, char** argv)
{
	int x=10;

	if (x>0)       // condition: check if variable x is greater than 0 // 10 is greater than 0. true
 
 	x++;             // x=x+1//x+=1
 	 
	printf("x = %d", x);
return 0;
}
